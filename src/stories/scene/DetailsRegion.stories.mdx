import DetailsRegion from '../../components/scene/DetailsRegion.vue'

<Meta title="Scene/DetailsRegion" component="{DetailsRegion}" />

export const Template = (args) => ({
  components: { DetailsRegion },
  setup() {
    return { args };
  },
  template: '<DetailsRegion v-bind="args"/>',
});

<Story
  name="Primary"
  args={{
    title: "title",
    description: "This is a short description",
    noButton: false,
    textColor: "black",
  }}>
  {Template.bind({})}
</Story>

# Details region

Simple button used to navigate accros pages.
