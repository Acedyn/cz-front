import TypographyTitle from '../../components/utils/TypographyTitle.vue'

<Meta title="Utils/TypographyTitle" component="{TypographyTitle}" argTypes={{
  size: { control: {type:"select", options: ["small", "regular", "big"] } } ,
  weight: { control: {type:"select", options: ["light", "regular", "bold"] } } ,
}} />

export const Template = (args) => ({
  components: { TypographyTitle },
  setup() {
    return { args };
  },
  template: '<TypographyTitle v-bind="args"><p>{{args.content}}</p></TypographyTitle>',
});


<Story
  name="Primary"
  args={{
    content: "Exemple",
    size: "regular",
    color: "#000000",
    weight: "regular",
  }}>
  {Template.bind({})}
</Story>


# TypographyTitle system

TypographyTitle system uses accros the website.
