import CloseButton from '../../components/interaction/CloseButton.vue'

<Meta title="Interaction/CloseButton" component="{CloseButton}" argTypes={{
      colorLine: { control: "color" },
      colorBG: { control: "color" },
  }} />


export const Template = (args) => ({
  components: { CloseButton },
  setup() {
    return { args };
  },
  template: '<CloseButton v-bind="args" />',
});


<Story
  name="Primary"
  args={{
    colorLine: "black",
    colorBG: "#664f3c",
    round: true,
    size: "30px",
  }}>
  {Template.bind({})}
</Story>

<Story
  name="Overlay close"
  args={{
    colorLine: "black",
    colorBG: "#664f3c",
    round: false,
    size: "40px",
  }}>
  {Template.bind({})}
</Story>


# Close button

Simple button used mostly to close popups.
